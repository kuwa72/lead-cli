# lead-cli project instructions

## Issue 開発フロー

Issue に着手するときは、次の順序を必ず守る。

1. `issue/<number>-<slug>` ブランチを作成する。
2. TDD で実装する。まず `test/` に失敗するテストを追加し、Red を確認してから実装して Green を確認する。ただし調査・RFC タスクはテスト実装不要。
3. ローカルテスト `./test/run-tests.sh` をすべてパスさせる。
4. base を `main` にして PR を作成する (`gh pr create --fill`)。
5. `bin/ci-wait <PR番号>` を実行して全 CI が pass するまで待つ。`gh pr checks` を手動で連打しない。
6. CI 成功後に PR をマージする (`gh pr merge --squash` または `--auto`)。
7. マージ後に関連 Issue が closed になったことと、マージコミットを確認する。

PR 作成や CI 起動だけでは Issue 対応を完了としない。CI 終了確認・マージ・Issue クローズ確認までを完了条件とする。

8. Issue ドリブンで作業する。PRをマージしたら、対応Issueをクローズするか、継続課題・未完了条件・次の作業をコメントして更新する。
9. 対応Issueに親またはトラッキングIssueがある場合は、子Issueの完了・保留・分割状況を親Issueにも反映する。
10. Issueをクローズする前に、関連PR、成果物、CI結果、親またはトラッキングIssueの更新を確認する。

## セッション管理ルール

作業セッションは PR 毎または Issue 毎に区切ることを想定する。前セッションのコンテキストは引き継がず、都度クリアする。

1. **着手時の起点**: 新 Issue 着手時は `main` 最新から新規ブランチ・新規セッションで開始し、前作業の未コミット差分や一時ファイルを残さない。
2. **完結単位**: 実装からテスト・PR・CI 待機・マージ・Issue クローズ確認までを同一セッション内で完結させる。
3. **引継ぎの明示化**: セッションを跨ぐ情報は Issue コメントまたは `docs/` に記録し、暗黙のコンテキスト引継ぎに頼らない。

## テスト品質および動作検証ルール（厳格遵守）

対話UIや外部コマンド呼び出しの不備（TTY競合、引数エラー等）を未然に防ぐため、以下のテスト・検証基準を義務付ける。

1. **コードの `grep` テスト原則禁止**:
   - スクリプト内に特定の文字列・フラグが存在するかを `grep` するだけのテストは**禁止**。単なる静的テキスト検査は機能が正しく動作することの証明にならない。
   - テストは必ず**振る舞い（実行引数、標準入出力、終了ステータス、生成された成果物）**をアサートすること。
   - CI では `test/test_no_source_grep.sh` が test スクリプトによる実装ソースの `grep` を検出する。共有ヘルパー・方針は `docs/testing.md` と `internal/testutil` を参照すること。
2. **外部コマンド（CLI）連携の事前確認**:
   - `agy`, `herdr`, `gh` などの外部コマンドを呼び出すコードを追加・変更する場合、必ず事前に `--help` や実機実行でオプション仕様（必須フラグ、位置引数の有無等）を確認すること。推測で引数を組み立てない。
3. **モック（PATH 注入 / ダミーコマンド）による振る舞いテスト**:
   - テスト内で外部コマンド（`herdr`, `gh`, `fzf` 等）を呼び出す場合は、テスト用の一時ディレクトリを作成してダミーの実行可能スクリプトを配置し、`PATH="$tmp_bin:$PATH"` で優先実行させる。
   - ダミーコマンド側で渡された引数（`"$@"`）や標準入力をログに記録し、テストスクリプト側でその記録内容を厳密に検証すること。
4. **実機・実コマンド確認の実施**:
   - テストスイート通過後、PR作成前に開発環境上で実際にスクリプトを実行し、期待通りの出力・画面遷移になることを確認すること（対話UIの場合は非対話テストモードやダミー入力を用いて実稼働確認を行う）。

